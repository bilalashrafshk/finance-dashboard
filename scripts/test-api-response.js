
async function testApi() {
  try {
    // Assuming the server is running on port 3000. If not, I can't test the API end-to-end easily without starting it.
    // But I can verify the logic by simulating the API handler code in a script.
    // Let's try checking the logic directly first to avoid server dependency.
    console.log('Skipping live API test, checking logic via direct function call simulation...');
  } catch (error) {
    console.error('Error:', error);
  }
}

testApi();
